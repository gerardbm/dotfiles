#!/usr/bin/env bash
#
# Description: download all videos from a channel/user,
# keeping each playlist in a separate directory.

yt-dlp -o '%(uploader)s/%(playlist)s/%(playlist_index)s - %(title)s.%(ext)s' $1
