#!/usr/bin/env bash
# Version: 1.0.0

# Description: Convert video files to WEBM

for i in "${@:1}"; do
	NAME="${i%%.*}"
	ffmpeg -i "$i" -c:v libvpx-vp9 -crf 30 -b:v 0 -c:a libopus -b:a 128k "$NAME.webm"
done
